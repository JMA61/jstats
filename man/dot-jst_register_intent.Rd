% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/7036Tools.R
\name{.jst_register_intent}
\alias{.jst_register_intent}
\title{Internal helper: shared registration engine for jnumeric() / jcount()}
\usage{
.jst_register_intent(kind, data, data_name, default_used, var_names, remove)
}
\arguments{
\item{kind}{One of "numeric", "count".}

\item{data}{The resolved data frame.}

\item{data_name}{Character data-frame name (the registry key).}

\item{default_used}{Logical; whether the \code{juse()} default frame was used.}

\item{var_names}{Character vector of variable names to register.}

\item{remove}{Logical; if TRUE, remove rather than write.}
}
\value{
\code{invisible(NULL)}.
}
\description{
Validates the requested variables, then either removes their registrations
of the given kind (\code{remove = TRUE}) or writes them, enforcing mutual
exclusion: writing a record replaces any prior intent record for that
variable (one record per variable in \code{.jst_registry}) and clears any
\code{.jst_dummy} registration for it. Any reclassification (a variable that
previously carried a different intent or a dummy registration) is reported.
A standard-tier reminder notes that registrations are session-only and how
to persist them.
}
\keyword{internal}
