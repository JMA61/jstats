% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/7036Tools.R
\name{jchisq}
\alias{jchisq}
\title{Chi-square test of independence with cross-tabulation}
\usage{
jchisq(
  formula,
  data,
  expected = FALSE,
  row.pct = TRUE,
  col.pct = FALSE,
  labels = TRUE
)
}
\arguments{
\item{formula}{A formula of the form \code{Row ~ Column}.}

\item{data}{A data frame containing variables referenced in \code{formula}.}

\item{expected}{Logical. If TRUE, prints expected frequencies alongside
observed. Default is FALSE.}

\item{row.pct}{Logical. If TRUE (default), shows row percentages.}

\item{col.pct}{Logical. If TRUE, shows column percentages. Default is FALSE.}

\item{labels}{Logical. If TRUE (default), prints variable labels
when available.}
}
\value{
Invisibly returns a list containing the cross-tabulation,
chi-square statistic, degrees of freedom, and p value.
}
\description{
Produces an SPSS-style cross-tabulation of two categorical variables
with observed frequencies, expected frequencies, row percentages,
column percentages, and a chi-square test of independence. Handles
haven-labelled, numeric, factor, and character variables.
For haven-labelled variables, numeric codes are displayed alongside
labels.
}
\examples{
jchisq(cyl ~ am, data = mtcars)
jchisq(cyl ~ am, data = mtcars, expected = TRUE, col.pct = TRUE)

}
