% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/7036Tools.R
\name{.jst_class_from_role}
\alias{.jst_class_from_role}
\title{Internal helper: map an asserted analysis role to class + subclass}
\usage{
.jst_class_from_role(role, x)
}
\arguments{
\item{role}{One of "numeric", "count", "categorical".}

\item{x}{The variable (used only to derive the categorical subclass).}
}
\value{
A list with \code{class} and \code{subclass}, or \code{NULL} if
\code{role} is not recognized.
}
\description{
Shared by the classification resolver's user-intent tiers (per-call
override and registered intent) so an asserted role produces the same
class/subclass pair however it was asserted. "numeric" and "count" fix the
subclass; "categorical" still takes its dichotomy vs N-category subclass
from the data structure, since the role assertion fixes the class but not
the category count.
}
\keyword{internal}
