# Internal helper: format the equation subtitle for a bivariate regression

Uses variable names rather than y/x for pedagogical clarity.

## Usage

``` r
.jst_format_bivar_equation(model, y_name, x_name, include_r2 = TRUE)
```
