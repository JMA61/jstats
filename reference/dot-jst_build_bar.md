# Internal helper: build bar chart (1 categorical variable)

Internal helper: build bar chart (1 categorical variable)

## Usage

``` r
.jst_build_bar(
  data,
  x_name,
  by_name = NULL,
  axis_labels = NULL,
  by_label = NULL
)
```
