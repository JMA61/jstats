# Internal helper: build boxplot (1 numeric + 1 categorical)

Internal helper: build boxplot (1 numeric + 1 categorical)

## Usage

``` r
.jst_build_box(
  data,
  variable_names,
  var_types,
  by_name = NULL,
  axis_labels = NULL,
  by_label = NULL
)
```
