# Internal helper: build grouped bar chart (2 categorical variables)

Internal helper: build grouped bar chart (2 categorical variables)

## Usage

``` r
.jst_build_grouped_bar(data, variable_names, axis_labels = NULL)
```
