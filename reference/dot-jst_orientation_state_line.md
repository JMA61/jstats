# Internal helper: one status line for a found orientation copy

Internal helper: one status line for a found orientation copy

## Usage

``` r
.jst_orientation_state_line(found_v, edited, regen_call)
```
