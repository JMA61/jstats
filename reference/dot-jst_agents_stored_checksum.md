# Internal helper: parse the stored checksum out of an end-marker line

Internal helper: parse the stored checksum out of an end-marker line

## Usage

``` r
.jst_agents_stored_checksum(end_line)
```
