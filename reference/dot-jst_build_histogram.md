# Internal helper: build histogram (1 numeric variable)

Internal helper: build histogram (1 numeric variable)

## Usage

``` r
.jst_build_histogram(
  data,
  x_name,
  by_name = NULL,
  axis_labels = NULL,
  by_label = NULL
)
```
