# Internal helper: human-readable label for a registered intent kind

Internal helper: human-readable label for a registered intent kind

## Usage

``` r
.jst_intent_label(kind, cap = FALSE)
```

## Arguments

- kind:

  One of "numeric", "count", "dummy".

- cap:

  Logical; if TRUE, capitalize the first letter.

## Value

A character label.
