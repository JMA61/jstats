# Internal helper: mask declared SPSS-form UDM cells to NA on analysis copy

Internal helper: mask declared SPSS-form UDM cells to NA on analysis
copy

## Usage

``` r
.jst_apply_declared_udms_as_na(data)
```
