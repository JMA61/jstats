# Table 4 (S284): which FORM the one-line N statement takes. Chosen by the layout's n_line_family and, for the pool family, whether the analysis variables' per-variable Ns differ. The excluded-count rider (appended whenever cases were excluded before the analysis) is orthogonal to the form and is applied by the renderer, not encoded here.

Table 4 (S284): which FORM the one-line N statement takes. Chosen by the
layout's n_line_family and, for the pool family, whether the analysis
variables' per-variable Ns differ. The excluded-count rider (appended
whenever cases were excluded before the analysis) is orthogonal to the
form and is applied by the renderer, not encoded here.

## Usage

``` r
.jst_cps_n_line_rules
```
