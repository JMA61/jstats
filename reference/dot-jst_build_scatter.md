# Internal helper: build scatterplot (2 numeric variables)

Internal helper: build scatterplot (2 numeric variables)

## Usage

``` r
.jst_build_scatter(
  data,
  variable_names,
  by_name,
  line,
  equation,
  r2,
  band,
  axis_labels = NULL,
  by_label = NULL
)
```
